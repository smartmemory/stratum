{
  "name": "@smartmemory/stratum",
  "version": "0.0.1",
  "private": true,
  "description": "Stratum engine — typed, resumable, postcondition-enforced workflows for AI agents (TypeScript port)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.11",
    "@ai-sdk/openai": "^4.0.11",
    "ai": "^7.0.19",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
