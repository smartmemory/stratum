# Plan-gate review round 1 (codex gpt-5.6-terra/high) — REVISE

All 9 files enumerated, no stale refs. 4 findings — the plan under-delivers the approved design; do NOT change the design, change the plan:

1. **P1 — sync-path discriminant validation missing.** Design requires runAgent() to validate agent/sandboxMode before foreground dispatch (design.md:286, :650); plan/blueprint limit runner.ts to optional pid + tool-filter forwarding (plan.md:285-317; blueprint.md:633-684). Current foreground dispatch sends every non-codex agent to claude (runner.ts:45-61). Add the validation step plus focused foreground rejection tests.
2. **P1 — worker stderr behavior unplanned.** Design's File Change Map requires claude-bg-worker.ts to write stream errors to stderr (design.md:648). Plan creates .err but the worker input carries no stderr path and error behavior only writes JSONL to stream.jsonl (plan.md:161-168, :258-266). Add stderr plumbing + an error-path assertion on stream.jsonl.err.
3. **P1 — D9 cancellation interleaving tests absent.** Design requires controlled callback-order tests: synchronous exit after terminate() proving exactly one rc=130 sentinel; exit-handler suppression when cancelling=true; error-handler suppression after cancellation; single registry deletion (design.md:711-712, :739-740). Plan has only generic cancel/error coverage (plan.md:469-491). Add worker-stub tests for those exact event orders.
4. **P2 — public-surface tests are prose, not tasks.** Design requires codex workspace-write start/poll via command seam, claude bg start through stratum_agent_run, MCP forwarding/rejection tests, foreground claude allowlist flow (design.md:681-682, :697-701, :750). Plan's new test file calls startBackgroundRun() directly and MCP checks are acceptance prose (plan.md:443-455, :347-350). Schedule the public MCP tests and the codex workspace-write command-seam flow as concrete test-file tasks.
