NOT CLEAN

Reviewed the uncommitted S5a diff and implementation report within the requested scope, against design §2/§4 (including the S5 delta) and plan AC03/AC13/AC16. Excluded concurrent-work paths were not reviewed. No repo files were edited and no paid model runs were started.

1. **MEDIUM (P2): The sandbox live gate trusts the exec baseline report without validating its raw runs.** `ts/tests/connectors/codex-appserver.live.test.ts:69` only requires referenced artifacts to be nonempty; lines 76–78 compare app-server observations with `report.json.runs[].observed`. The gate never parses the baseline command artifacts to require a successful exit, completed turn, successful probe tool execution, or agreement between tool results and the reported observations. Thus a failed or tool-less exec baseline can still certify temp parity, contrary to the requested independent evidence reassessment.

   **Repro:** `/tmp/peer3-s5a-evidence-mutations.py` copies the supplied evidence and changes all three raw exec baseline runs to `code: 1`, `reason: "timeout"`, and empty `stdout`, leaving the report unchanged. Running the exact S5a live gate against `/tmp/peer3-s5a-review-mutants-t5zslo5f/baseline` still returns **3 passed**, exit 0. Output: `/tmp/peer3-s5a-mutant-baseline.log`. The original supplied baseline is successful; this is a gate defect, not a claim that that run failed.

   **Suggested fix:** Reassess each baseline artifact from its exit/reason and parsed stdout, require one successful probe command and successful terminal turn, and compare its result with the recorded host observations and app-server result. Reject report errors and inconsistent metadata. Add negative tests that mutate the raw artifacts while leaving the report positive.

2. **MEDIUM (P2): Process evidence assessment accepts missing active-turn evidence and unauthenticated/uncorrelated pending-steer settlement; the new tests do not protect the steer-specific rule.** `ts/scripts/peer3-probe.mjs:599` trusts `activeBeforeAction` without checking retained turn records. Lines 605–609 require a steer request and a dropped callback but ignore the recorded callback authentication and the matching driver-to-sidecar `steer-result`. The gate also does not enforce the native/shim label. A case can therefore pass with evidence contradicting the authenticated, correlated pending-steer claim. The unit test at `ts/tests/connectors/codex-appserver.live.test.ts:122` explicitly accepts an active cancellation with no records, and its pending-steer negative case fails at the earlier missing-barrier check, so it never exercises the steer-specific checks.

   **Repro:** In `/tmp/peer3-s5a-review-mutants-t5zslo5f/process`, the scratch mutation removes all records from cancel-active and driver-sigkill, sets pending-steer callback authentication to false, removes its steer-result IPC, and mislabels that shim run as native. The exact live gate still returns **3 passed**, exit 0 (`/tmp/peer3-s5a-mutant-process.log`). Separately, deleting the entire pending-steer-specific assessment block in a scratch module leaves the existing evidence test file green: **16 passed, 4 skipped** (`/tmp/peer3-s5a-rule-mutant.log`). No repository source was mutated.

   **Suggested fix:** Validate active-turn records and recorded action ordering; require authenticated callback framing and correlate the sent message, steer request, expected active turn, steer-result request ID, and dropped/unknown callback. Enforce the expected shim/native classification. Add otherwise-valid pending-steer fixtures that independently remove or contradict each required observation, so removing any check fails a test.

The production policy change itself has no identified defect in this review. The generated 0.155.1 `ThreadStartParams` accepts `sandbox` plus JSON-valued `config`, and `SandboxMode` contains the three emitted modes. The encoder preserves exec argument order and serialization, emits workspace roots/network overrides only for workspace-write on app-server, and omits temp overrides and turn sandboxPolicy. Frozen argv tests pass; no production path sending turn/start.sandboxPolicy remains. Full-access authorization remains in place.

The supplied live observations are substantive: read-only denies actual writes with EPERM; workspace-write allows writes and denies the OS-writable home-directory target; network-on executes the probe and returns HTTP 200 while network-off returns ENOTFOUND; temp-override denies both temp locations while permitting the selected extra root. In particular, network-on and the extra-root write under temp exclusions discriminate against silently ignoring the dotted config overrides. These observations establish the tested configuration, not every possible ambient installation.

The actual pending-steer artifact contains a client turn/steer, held response, matching steer-result IPC, and authenticated dropped/unknown callback. Process checks occur before emergency cleanup; native/shim cases are labeled in the original evidence. Targeted cleanup uses captured identities, and production cancellation retains its designed process-group semantics. The reported Python shutdown error is confined to the controlled shim. Inspection found no retained peer-token/auth-secret fields or unredacted unrelated command lines. AC16's full release/golden/server-request rerun remains explicitly deferred to S5b; it is not claimed complete here.

Verification:
- `npm --prefix ts run build`: passed (`/tmp/peer3-s5a-build.log`).
- `npm --prefix ts run typecheck`: passed (`/tmp/peer3-s5a-typecheck.log`).
- Exact requested eight-file Vitest run: **8 files passed; 542 passed, 4 skipped** (`/tmp/peer3-s5a-tests.log`).
- Exact S5a live gate against `/tmp/peer3-s5a-full-154454`: **3 passed**, exit 0.
- `STRATUM_TEST_ALLOW_SOCKET_EPERM` was not set. Socket tests ran successfully.
