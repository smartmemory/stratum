# STRAT-AGENT-PEER-3 progress ledger

Recovery map: this file + `git log --oneline -- docs/features/STRAT-AGENT-PEER-3 ts/`. Plan: [plan.md](./plan.md). Design: [design.md](./design.md).

Loop per slice: Codex astra/medium implements → controller verifies (targeted tests, unsandboxed) → Codex astra/medium review → fix → controller commits. Full suite only at baseline and S5 end.

## Commits
- `148af46` design r3 + owner decisions
- `430ecd5` side fix: background Codex polls report cached tokens + estimated USD
- `af99636` plan (5 slices) + design corrections

## Pre-slice probes (2026-09-25)
- node v22.22.3, codex-cli 0.155.1.
- `initialize` → `{"userAgent":"stratum-peer3-probe/0.155.1 (Mac OS 26.7.0; arm64) iTerm.app/3.6.11 (stratum-peer3-probe; 0.1.0)","codexHome":…,"platformFamily":"unix","platformOs":"macos"}`. Version = segment after the first `/` in userAgent, up to the first space. The prefix before the `/` is the client name we send.
- Idle app-server on stdin EOF: exits rc 0 within 3s. Active-turn EOF is still to be proven in S5.
- dist rebuilt at `430ecd5` (cost fix in `ts/dist/connectors/background.js`). The MCP server process still holds old modules until it reconnects.

## Full-suite baseline (before S1)
- Run on a clean `git archive af99636` export, with Stripe/Resend env unset: 1803 passed, 2 failed, 3 skipped. The 2 failures (`cli/distill` git-root discovery, `mcp/flow_cancel` T-S03-3) are environmental: the export is not a git repo, and load was ~6.6 with 2 Codex jobs running. Both pass in the real repo (26/26). Baseline treated as green.

## Live probe evidence (2026-09-25, luna/low, disposable CODEX_HOME)
- **sandbox-baseline (exec):** in workspace-write with default config, workspace, extra writable root, `$TMPDIR` and `/tmp` are all writable. This matches design §4 `excludeTmpdirEnvVar:false, excludeSlashTmp:false` for exec. App-server parity is still S5 AC03. Override run recorded in `sandbox/override.json`.
- **server-requests (rc=1 by design: fails on unexercised methods):** real handshake identity `stratum/0.155.1 (Mac OS 26.7.0; arm64) iTerm.app/3.6.11 (stratum; 0.1.0)` passed the gate. `item/commandExecution/requestApproval` → `{decision:"decline"}` accepted (server logged "rejected by user"), run `completed`. `mcpServer/elicitation/request` → decline accepted, run `completed`. **9 methods not elicitable in this config** (fileChange approval, legacy execCommand/applyPatch approvals, permissions, tool/requestUserInput, auth refresh, attestation, tool/call, unknown). For those, AC05 rests on generated-type checks, the strict fake, and the 120s stall watchdog. **Controller adjudication:** AC05 accepted with this documented gap; S5 reruns the probe.
- **server-requests rerun after the S2 fixes (rc=0):** proven on the real server: `item/commandExecution/requestApproval`, `item/fileChange/requestApproval` (new dedicated trigger: on-request + read-only + patch prompt), `mcpServer/elicitation/request`. Unreached, each with its trigger recorded: execCommandApproval, applyPatchApproval (legacy, not sent by the modern server even when prompted), item/permissions/requestApproval, item/tool/requestUserInput, auth refresh, attestation, item/tool/call, unknown. Live evidence gate `STRATUM_LIVE_PEER3=1 … codex-appserver.live.test.ts`: 9/9. Evidence: `peer3-evidence/requests2/`.
- Evidence dir: `/private/tmp/claude-501/-Users-ruze-reg-my-forge/a0b33df7-0e94-469a-b181-8b5ea924ff6d/scratchpad/peer3-evidence/` (scratch, not durable; S5 report retains a copy).

## S5a live runs (2026-09-25, luna/low, evidence /tmp/peer3-s5a-152309, scratch)
- preflight: 6/6 passed (codex 0.155.1 = pin, login, node, OS, HEAD, claude version).
- sandbox: 6/7. read-only, workspace-write (root allowed, outside denied), network on/off, temp built-in and temp ordinary all match exec. **temp-override FAILED, real defect:** a profile setting both temp exclusions gives exec EPERM on $TMPDIR and /tmp, but app-server writes both, because `turn/start.sandboxPolicy` requires the exclude fields and codex-policy.ts forced `false`. App-server was wider than exec. Design delta recorded in design.md §4: move sandbox to thread/start `sandbox` + `config` (same dotted keys as exec -c) and send no turn sandboxPolicy. Fix dispatched (astra/medium); proof = sandbox probe 7/7 rerun.
- **Temp-exclusion fix (uncommitted):** codex-policy.ts now sends `thread/start.sandbox` + `config` (one shared object feeds exec -c and app-server config; exec argv byte-identical) and no turn sandboxPolicy. Controller: typecheck, build, 9 files 535 passed / 4 live-skipped. Live sandbox rerun (/tmp/peer3-s5a-fix-153501): app-server temp-override now observes TMPDIR=false, /tmp=false, identical to exec (EPERM both), so **behavior fixed**. The case still reports failed due to a probe bug (expected hardcodes temp writable=true, so it throws before the parity comparison). Probe fix folded into the pending-steer diagnosis dispatch (same file, sequential).
- **Pending-steer diagnosis:** probe bug, not production. The probe's callback `uds:<root>/absent-callback.sock` fails `peer-registry.ts:isAllowedCallback` (`^\d+\.sock$`), so the sidecar rejected it before admission/IPC ("peer frame rejected: invalid callback or msg_id"). Probe now binds a PID-named callback, records replies/IPC/stderr, and fails loudly on refusal. New real-socket regression reproduces the old frame. Single-case live rerun passed (1 paid run): steer IPC with expectedTurnId → client `turn/steer` → held response → cancel → `dropped/unknown` callback. Cancel poll `error/child_died_without_sentinel` matches cancel-active/initialize (existing semantics). Probe temp expectations now derive from the exec baseline (`sandboxExpectations`).
- **Full live rerun (controller, /tmp/peer3-s5a-full-154454):** preflight 6/6, **sandbox 7/7** (temp-override now passes = temp fix proven), **process-tree 8/8**, live gate `S5a real production evidence` 3 passed. S5a review dispatched (astra/medium, full access, no edits; diff hash snapshotted).
- **S5a review r1** (impl-review-s5a-r1.md; full access, no edits: fingerprint excluding ledger unchanged) NOT CLEAN, 2 medium, both evidence-gate honesty: (1) sandbox gate trusts exec baseline report.json, not raw runs (mutant with all baseline runs timed out/empty still 3 passed); (2) process assessment trusts activeBeforeAction, ignores callback auth and steer-result correlation and the shim/native label (mutant still passed; deleting the pending-steer block keeps tests green). Production policy change CLEAN, and the live observations discriminate (network-on HTTP 200 vs off ENOTFOUND; extra root writable under temp exclusions, so dotted config keys are honored). Both accepted; fix dispatched (gate/tests only, no paid runs).
- **S5a r1 fixes:** gates re-derive from raw artifacts (exec baseline raw runs; active-turn records + action ordering; pending-steer msg_id → steer IPC(expectedTurnId) → steer-result → authenticated dropped/unknown callback; shim/native labels). 32/32 scratch check-deletions fail a test. Old process evidence correctly fails the new gate (lacked action.kind/time/records), so it was recaptured. Controller: typecheck, build, 5 files 469 passed / 4 live-skipped; **fresh live run /tmp/peer3-s5a-final-160146: preflight 6/6, sandbox 7/7, process-tree 8/8, gate 3 passed**; reviewer's mutant roots now FAIL (baseline 2 failed, process 1 failed). Accepted without r2 (narrow, gate-only; reviewer repros now fail). **S5a committed** (see git log).
- process-tree: 7/8 passed (cancel-initialize, cancel-active, sigterm-ignore shim, parent-death, driver-sigkill, completion-wins, cancellation-wins). **cancel-pending-steer UNREACHED:** the probe wrote auth+user frames straight to the sidecar socket (callback `uds:<absent>`, reply ignored), and the driver never sent `turn/steer`; the turn ran to completion at 35.6s. Cause unknown: probe framing/callback rejection vs a real steer-path defect. Diagnose after the temp fix (live, sequential).

## S5b live runs (2026-09-25, luna/low, evidence /tmp/peer3-s5b-162753, scratch)
- S5b implemented (golden start/collect, verify-evidence, fixtures; 30/30 check deletions fail a test). Controller: typecheck, build, live test file 111 passed / 6 live-skipped.
- Fresh root: preflight OK, sandbox 7/7, process-tree 8/8, server-requests rc 0.
- **Golden run 1** (peer `codex-luna-1a10931fc7c7-golden-b457496ca690`): this Claude session called ListAgents (peer listed, busy) → SendMessage(marker, notify_when_idle) once. Substantive chain PROVEN: marker absent from prompt; one turn; rollout carries the marker 5x as model-visible input; final text has the marker; poll complete, input 110704 / output 232, est $0.0027; wire: user frame msg_id 8ab6ad70 → `peer_message_status delivered` (same id); notify_when_idle(from_mode bypass) → one `peer_idle_notice` from_mode bypass; post-completion request `expired/refused`. Controller got "[Cross-session delivery notice] … approved and released … (recipient: uds:/tmp/cc-socks/38989.sock)" and one idle notice.
- Collect FAILED on a gate-shape assumption only ("Golden delivered callback, not ack"): the gate wanted `status: delivered` + from_mode inside the SendMessage tool_result, but installed Claude Code returns a send ack there, records delivery as a separate isMeta "delivery notice", and shows from_mode only on the wire. The delivered frame carries no top-level from_mode because Claude Code's msgV1 user frame puts from-mode inside the wrapper text; the sidecar echoes it only when present (not a bug). Gate fix dispatched: correlate tool_result msg_id = wire delivered id, notice recipient = peer socket, one real idle notice (queue-op lines not counted), from_mode on the wire. A fresh golden run is needed after (sidecar linger ended).

- **Golden gate fixed to the real transcript shape**, golden run 1 set aside (`evidence/…/golden-run1-gateshape`). **Golden run 2** (run 7b0777c9206b, msg f6dd8e63): collect rc 0. verify-evidence first flagged AC05 unreached and AC16 failed because it demanded every server-request method proven, contradicting owner-amended AC05. Scoring aligned: AC05 `passed-amended` only when every elicited method is proven and every unreached method has a recorded trigger; per-method statuses are never relabelled. verify-evidence exit 0 (preflight/AC03/AC13/AC07/AC16 passed, AC05 passed-amended 3 proven + 8 unreached); opt-in live file 139/139.
- Durable evidence copied to `evidence/2026-09-25-live/` (98 files, 1.8 MB; secret/email/account scans clean except plan_type "pro"; rollouts hold Codex's standard built-in developer prompt only). verify-evidence against the durable copy exits 0. report.md, README section and CHANGELOG entry written by the controller (claims checked against code/evidence).
- **S5b review r1** (impl-review-s5b-r1.md; no edits, fingerprint unchanged) NOT CLEAN: 2 HIGH (marker causality not tied to the steer, since the marker in startup context still passed; delivery notice not attributable to the marker message when another message goes to the same peer), 3 MEDIUM (post-completion refusal ignores contradicting wire; unreached list lost when AC05 fails; individual refusal conditions untested). All gate-only; all accepted; fix dispatched.

- **S5b r1 fixes** (gate only): marker tied to the steer IPC text within the one turn, and absent from all earlier model-visible context and from the exact original task; single-message window per peer (the renderer's delivery notice has no msg_id); post-completion refusal consistent across second.json and wire, with no conflicting outcome and no later steer/turn; the per-method AC05 report is always printed, even when AC05 fails; individual-condition tests. Codex: 172 passed / 6 skipped, evidence-enabled 178, reviewer mutants 14/14 rejected, individual deletions 16/16. Controller: durable evidence untouched; typecheck, build, verify-evidence on durable copy rc 0, opt-in live file 178/178; reviewer control passes; marker-in-prompt, marker-in-startup-context, other-message-delivery, post-completion-wire-accepted and conflicting-denied fail for the right reason; bad-server-reply fails AC05 and still lists 8 unreached. Accepted without r2.

- **Final full suite** (controller, once, Stripe/Resend env unset, no concurrent run): 121 files passed / 2 skipped, **2525 passed / 9 skipped, 0 failed**. **S5b committed** with evidence, report.md, README and CHANGELOG (see git log). Feature completion recorded through compose after the commit.

## Slices
| Slice | Status | Commit | Notes |
|---|---|---|---|
| side: cache_write key | DONE | `6638c5e` | luna/medium; 34 targeted passed (controller rerun) |
| S1 protocol pin + policy encoder | DONE | `c966ffc` | review s1-r1 NOT CLEAN (2), both fixed. Verified on isolated HEAD+S1: typecheck, build, pin --check, 416/416. prepare-dist staged without S2's driver-entry line. peer3-probe.mjs deferred to the S2 commit. **S2 must switch to `assertAppServerIdentity(userAgent, clientInfo)`**, since it was written against the old 1-arg form. |
| S2 driver vs fake app-server | DONE | (this commit) | r1 NOT CLEAN (6) → fixed; r2 NOT CLEAN (1 new HIGH: exit × backpressure drops buffered completion, reproduced) → fixed. Controller accepted without r3: narrow fix, the reviewer's own repro now gives completed 200/200 with one sentinel 0, locked as a regression test. Verified: typecheck, build, 175 passed / 1 skipped. Live gate 9/9 (requests2): 3 methods proven, 8 unreached with triggers. Design AC05 amended. |
| S3 sidecar owner/auth/reservations | DONE | `c389a93` | r1 NOT CLEAN (2 test defects) → fixed. Controller unsandboxed without flag: 30/30, no skips. Reviewer's mutant now fails exactly the 2 targeted AC11 tests. Committed together with S4, which edited S3's codex-appserver-driver.ts and codex-appserver-ipc.ts. |
| S4 selector + launch | DONE | `c389a93` | Codex: 113 passed, 5 sandbox-blocked. Controller unsandboxed: 306/308; 2 real failures (driver main exited 0 silently without IPC bootstrap) → fixed (non-zero exit + stderr, no stream/sentinel; tests use real bootstrap) → 314/314. Combined S3+S4 review r1 (impl-review-s34-r1.md; reviewer ran with full access, diff hash unchanged before/after) NOT CLEAN (1 medium): peer.json name+pid evidence check unprotected by tests, mutant claims a forged name. Fixed (impl-review-s34-r1-fixes.md): wrong-PID and wrong-name negative tests, each proven to fail under its half-condition mutation. Reviewer's one-off successful-registration flake: deadline expiry reproduced with a 3s-delayed sidecar vs the 2s budget; historical cause not recoverable (no stderr kept). Success test now uses a 15s budget patched into a disposable package copy only; no env knob, production stays 2s. Accepted without r2: narrow test-only fix, reviewer's mutation now fails. Controller verify: typecheck, build; under load avg 60+ (bfs/Spotlight/Docker, external) failures rotated between runs (317/318, then 12/12 isolated x2) = load signature; rerun at load <10: **318/318, 13 files, no skips**. |
| S5 live exit gate | DONE | | Split into S5a (preflight/sandbox/process-tree probe modes + live test gate: AC03, AC13) then S5b (golden flow with this Claude session doing ListAgents→SendMessage, verify-evidence, report/README/CHANGELOG, full suite: AC07, AC16). Sequential, same files. S5a dispatched (astra/medium); Codex cannot run live modes (nested sandbox), controller runs them unsandboxed at luna/low. |
