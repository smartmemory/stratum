NOT CLEAN

Reviewed the uncommitted S5b probe, live tests and fixture against design.md, plan.md SLICE 5, and implementation-s5b.md, including its real-transcript and amended-AC05 corrections. HEAD: ff8e983e9cdc11cd3613dcfc0548aac00828950e. No repository source files edited, paid model runs started, or Claude sessions accessed. Evidence experiments used copies under /tmp.

1. **HIGH — The marker can already be in the actual initial model context and AC07/AC16 still pass.** Evidence: `ts/scripts/peer3-probe.mjs:937,949-952` checks the separate prompt.txt for the marker, but only requires that a rollout user message *contains* that prompt and that another user message contains the marker. In the durable-evidence copy `marker-in-actual-original-prompt`, appending the marker to the rollout's original task passes. More decisively, `marker-only-in-startup-context` removes the peer-injected rollout message entirely and puts the marker in the initial environment/plugin user message; the full verifier still returns passed=true. Thus output need not have changed because of the steer. Suggested fix: correlate the rollout message to the actual IPC steer text and its position/time in the active turn; require the actual task input to match the captured prompt and reject marker occurrences in all earlier model-visible context. Add both mutations as negative tests.

2. **HIGH — A delivery notice for a different message to the same peer can satisfy the golden gate.** Evidence: `ts/scripts/peer3-probe.mjs:915-917,958-969` selects a notice by recipient socket, while counting only marker-bearing SendMessage uses. It does not rule out another message to that peer. In `other-message-delivery`, I added a non-marker SendMessage use/result and corresponding incoming/outgoing wire frames with another message ID, leaving one recipient-only delivery notice. The CLI still exits 0. That notice can represent the other message; the marker's outgoing callback does not independently prove controller receipt. Suggested fix: because this renderer omits message IDs, enforce an unambiguous single-message window for the controller sender/peer, or retain a controller-side receipt carrying the marker message ID. Reject ambiguous transcripts and test two messages to the same recipient.

3. **MEDIUM — Post-completion refusal ignores contradictory raw wire evidence.** Evidence: `ts/scripts/peer3-probe.mjs:982` accepts any matching expired/refused callback in second.json without correlating it to the captured wire exchange or rejecting conflicting outcomes. In `post-completion-wire-accepted`, changing only the second request's outgoing wire status from expired to delivered still passes AC07/AC16. In `post-completion-conflicting-denied`, appending a denied callback and an authentication-failure sender reply also passes. A simple replacement with denied correctly fails; the defect is accepting contradictory artifacts, not treating an ordinary denial as expiration. Suggested fix: require one consistent sent request and refusal across second.json and wire.json, reject conflicting outcomes/errors, and verify no later steer/new turn for that message.

4. **MEDIUM — The unreached-method list disappears when AC05 fails.** Evidence: `ts/scripts/peer3-probe.mjs:1197-1209` populates methods only after all runs validate and assessServerRequests returns. In `bad-server-reply`, replacing one elicited approval reply with an invalid result correctly fails AC05/AC16, but methods becomes empty. The actual CLI prints `[]`, losing all eight previously unreached methods and their attempted triggers. This violates the requested always-visible gap reporting and amended AC05's explicit method listing. Suggested fix: build all expected method records independently, retain triggers and proven results, and mark failing methods without aborting the entire method report.

5. **MEDIUM — Whole-guard deletion tests miss individual post-completion requirements.** Evidence: `ts/tests/connectors/codex-appserver.live.test.ts:308-310` tests status, authentication and lateness, but not the refusal's message ID, peer socket or status_detail. After a passing unchanged scratch control, individually deleting each of `p.orig_msg_id === second.sentFrame?.msg_id`, `p.from === uds:<peer socket>`, and `p.status_detail === 'refused'` from the refusal predicate still produced 133 passed / 6 skipped. These are actual lost rules despite the report's successful whole-statement mutation tests. Suggested fix: mutate each correlation/detail field in otherwise-valid evidence and assert rejection; use individual-condition mutations as well as whole-guard deletions.

Validation: build and typecheck exited 0. The requested ordinary Vitest command passed 133 tests with 6 skipped; the requested evidence-enabled command passed all 139. The untouched durable bundle's verify-evidence CLI exited 0, reporting AC05 passed-amended (3 proven, 8 unreached) and AC16 passed. Negative evidence mutations correctly rejected a second rollout turn, an assistant-role marker, zero input tokens, mismatched from_mode, wrong-recipient delivery, queue-only idle, a simple auth denial, mismatched HEAD, and an invalid elicited reply. Summary files were left unchanged in these experiments; their original passing outcomes did not mask those failures.

Transcript/security check: the current golden controller artifact contains only one SendMessage use, its result, delivery notice and idle notice; the archived first-run artifact contains its use/result/idle only. No unrelated controller prose was found. Credential-pattern and populated-secret-field scans across the durable bundle found no apparent credentials, bearer/API tokens, JWTs or private keys. Codex rollouts retain their own standard developer/environment context and probe conversation; these are distinct from controller transcript extraction. This is a content scan, not a guarantee against every possible secret format.

Reproduction artifacts:
- `/tmp/peer3-s5b-review-mutate.mjs` and `/tmp/peer3-s5b-review-mutations/results.json` (the same-peer scenario was additionally extended with controller use/result blocks after this script ran; its final CLI result is below).
- `/tmp/peer3-s5b-review-other-message-cli.log` and `/tmp/peer3-s5b-review-bad-server-cli.log`.
- `/tmp/peer3-s5b-review-rule-deletions/{control,second-message-id,second-peer-socket,second-refusal-detail}.log`.
- `/tmp/peer3-s5b-review-{build,typecheck,tests,live-tests,verify}.log`.
