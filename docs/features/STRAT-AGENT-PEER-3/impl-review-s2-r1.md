NOT CLEAN

Scope: uncommitted Slice 2 against design.md §§2–4 and plan.md AC05/AC06/AC12. Repository files were not edited. Paths below are relative to the repository unless absolute.

1. **HIGH — Process exit is not a terminal-claim trigger; cleanup can hang on inherited pipes.** Evidence: `ts/src/connectors/codex-appserver-driver.ts:220–232` listens for `close` and stdout `end`, but not process `exit`; `:131–132` also waits for `close` after SIGKILL. A child can exit while a descendant keeps stdout/stderr open. After turn startup, neither the startup timeout nor an unconditional runtime timeout protects this case. This violates §2's explicit process-exit failure rule; waiting for descendant pipe closure also exceeds the driver/app-server cleanup scope. A temporary injected-transport reproduction emitted `exit(1)` before `turn/completed` and received `completed` plus sentinel 0. Existing exit fixtures (`fake-app-server.mjs:49–50`) close the pipes too, masking the distinction. **Fix:** track process exit/reaping separately from stdio closure, claim failure on exit without an existing claim, and bound/dispose inherited streams during cleanup. Test exit with pipes held open and assert failure, reap, and exactly one sentinel.

2. **MEDIUM — Three malformed notification payloads do not trigger transport failure.** Evidence: `ts/src/connectors/codex-appserver-driver.ts:199` resets the malformed counter before notification processing; exceptions at `:180`, for example, increment it back to only one at `:212`. Reproduction: send three `item/completed` notifications with `item:{type:"fileChange",changes:{}}`, followed by completion. All three log “malformed app-server frame”, yet the result is `completed`, sentinel 0. The tests at `ts/tests/connectors/codex-appserver-driver.test.ts:68–79` exercise invalid JSON only. **Fix:** validate/process a frame successfully before resetting the counter, while distinguishing transport/send errors from malformed input. Add consecutive malformed-payload and valid-frame-reset tests. Tighten envelope validation too: `{method:123}` currently silently resets the counter.

3. **HIGH — AC05 real-server acceptance remains unmet.** Evidence: `docs/features/STRAT-AGENT-PEER-3/plan.md:140` explicitly requires real-server evidence before accepting S2. `ts/scripts/peer3-probe.mjs:193–196` supplies only two model prompts; its gate at `:244–245` correctly reports missing methods. The existing report at `/tmp/claude-501/-Users-ruze-reg-my-forge/a0b33df7-0e94-469a-b181-8b5ea924ff6d/scratchpad/peer3-evidence/requests/report.json:15–24` lists nine gaps: file approval, both legacy approvals, permissions, user input, auth refresh, attestation, tool call, and unknown. Running the opt-in test against that report fails at `ts/tests/connectors/codex-appserver.live.test.ts:12`. **Fix:** obtain the required evidence using suitable real-server configurations/triggers, or explicitly resolve unreachable-method requirements in the design/acceptance criteria. Do not accept the fake suite as completion of AC05.

4. **MEDIUM — stdout consumption bypasses writer backpressure.** Evidence: `ts/src/connectors/codex-appserver-driver.ts:80–82` appends each output record to an unbounded promise chain; `:225–230` continues consuming stdout independently. The 1 MiB frame limit bounds individual frames, not retained records. A slow/stalled writer plus many legal completed-item frames can consume unbounded memory; cleanup then waits on that entire queue before closing stdin (`:125–126`). **Fix:** introduce a bounded record queue and pause/resume stdout at watermarks, or an explicit bounded-overflow failure policy. Test a held writer and a producer exceeding the queue limit, then release it and verify ordering and the single sentinel.

5. **MEDIUM — Several required rules lack tests sensitive to their removal.** Evidence: `ts/tests/connectors/codex-appserver-driver.test.ts:61–63` checks only the type names of three progress items, so removing command/output/exit-code, file-change details, or MCP result/error mappings still passes; reasoning/web-search mappings at driver `:182–183` have no assertions. Cleanup tests `:85–94,159–172` expedite waits or make stdin finish immediately emit close (`:101–102`); they do not pin the unsignalled 3s → SIGTERM → 2s → SIGKILL schedule. No test deliberately splits a valid frame or a multibyte UTF-8 character across chunks. **Fix:** assert complete mapped record shapes, add deterministic chunk-boundary tests, and use fake time plus an independently controlled child to assert each cleanup deadline and reap-before-sentinel. These should fail when the relevant mapping, decoder, wait, or escalation is removed.

6. **LOW — Client identity advertises a stale, duplicated version.** Evidence: `ts/src/connectors/codex-appserver-driver.ts:240,242` duplicates `{name:"stratum",version:"0.1.0"}`, while `ts/package.json:3` is `0.6.1`; `ts/tests/helpers/fake-app-server.mjs:30` hard-codes the same stale echo. This does not currently break the Codex version gate, because the server echoes the supplied client version, but it misidentifies the client and can drift between request and validation. **Fix:** share one clientInfo value backed by shipped package-version data (or document a deliberately independent driver protocol version), and have the fake echo/check the actual initialization parameters.

Verification: targeted driver suite passed **37/37**; ordinary live suite skipped **1/1**. Explicit opt-in without evidence failed with “STRATUM_PEER3_EVIDENCE is required”; opt-in with the existing report failed for the unresolved methods. No paid model turns were started. Temporary transport reproductions are in `/tmp/peer3-s2-check.mjs`.

Checks that held: the fake validates entire server-response objects with `assert.deepEqual` and exits 42 on mismatch (`fake-app-server.mjs:59–63`), including nullable elicitation fields and legacy denial shapes. It requires `initialized` before thread startup (`:32–34`). Tests cover all three handshake RPC errors, spawn failure, startup/stall deadlines, cumulative usage, completion→signals, signal→interruption, late output suppression, and sentinel count/value in those races. Unknown notifications are deliberately ignored; stderr is consumed separately from exec records. The driver-entry prepare-dist change introduces no finding.
