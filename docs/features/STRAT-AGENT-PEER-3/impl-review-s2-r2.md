NOT CLEAN

1. **HIGH — The exit fix races the new backpressure buffer, discarding received output and completion.** At `ts/src/connectors/codex-appserver-driver.ts:85,239–244`, reaching 64 queued records pauses parsing, leaving complete frames in `buffer`. The process-exit handler at `:254` immediately claims failure even if that buffer already contains `turn/completed`. Once claimed, `:89,241` prevent further consumption, and cleanup erases the buffer at `:148`. A slow writer therefore changes a successfully completed turn into failure and loses preceding output. The named F4 test misses this because its memory child exits only after the driver closes stdin, after parsing completion.

   **Evidence:** `/tmp/peer3-r2-buffered-exit.mjs` uses the injected transport and delivers one chunk containing turn start, 200 items, and successful completion while holding the writer. It then emits process exit 0 and releases the writer. Actual result: `claim:"failed"`, only **62/200** items, error `app-server exit before turn/completed`, exactly one sentinel **1**. All frames, including completion, were delivered before exit.

   **Fix:** separate observed process exit/reaping from finalizing transport failure. Drain already-received complete frames under the existing queue bound before deciding whether exit lacked completion; bound handling of remaining pipe data so inherited pipes cannot restore F1's hang. Add a held-writer + buffered-completion + process-exit regression asserting all 200 items, completion, and one sentinel 0, while retaining the grandchild-held-pipes failure test.

R1 fix and named-test audit (sensitivity assessed from code; no repository edits or temporary repo reverts):

| R1 | Fix disposition and test sensitivity |
|---|---|
| F1 | Original inherited-pipe hang resolved: exit records reaping and cleanup destroys streams independently of close. `process exit with grandchild-held pipes fails, reaps and emits one sentinel` would fail/time out if exit handling or stream disposal were reverted. New interaction is finding 1 above. |
| F2 | Resolved: reset follows successful processing; envelope checks reject the additional malformed cases. All six `three consecutive malformed payloads fail: %j` cases detect their corresponding rollback. **`a processed valid frame resets malformed payload count` would still pass the original reset-before-processing implementation**; it is a complementary guard against removing reset entirely, not an independent reproduction of F2. The consecutive-malformed test supplies that coverage. |
| F3 | Resolved under the amended AC. `evidence gate records unreached methods and requires a dedicated patch approval trigger` detects removal of the dedicated trigger, omission/mislabeling of unreached methods, or restoration of the all-methods-proven requirement. The seven `evidence gate checks every elicited reply: %s` cases exercise failed/cancelled runs, absent completion, post-reply error, bad/missing reply, and successful proof; removing their respective validation fails the relevant negative case. A wholesale rollback to the old reject-all-gaps gate can still pass negative throw assertions, but fails the positive cases. Existing `requests2` real transcripts pass the opt-in gate: command approval, **file-change approval**, and MCP elicitation proven; eight methods explicitly unreached with triggers. |
| F4 | Queue growth resolved: 64-record watermark, resume below 32, bounded decoded input. `bounded queue pauses a held writer and resumes in order beyond the watermark` detects removal of queue/parser gating, pause/resume, or ordering. It does not cover process exit while paused; finding 1 is that regression. |
| F5 | Coverage gap resolved. `every section 3 mapped record has its complete exec shape` detects removal of mapped fields, including reasoning, web search, MCP results/errors, and command defaults. `decodes a valid %s split across chunks` detects loss of partial-frame buffering (`frame`) or UTF-8 decoder state (`utf8`). `unsignalled cleanup waits 3s then SIGTERM then 2s then SIGKILL and reap before sentinel` detects removed/changed deadlines, missing escalation, or sentinel-before-reap. These protect existing behavior; F5's fix adds sensitive assertions rather than changing those mappings. |
| F6 | Resolved. `initialize and identity validation use the shipped package version` detects reverting either initialization or identity validation to 0.1.0. The published path is valid: `dist/connectors/../../package.json` resolves to package-root `package.json`, which npm includes despite `files:["dist"]`. A scratch pack using the actual manifest and client-info module included both files; importing the extracted module returned `{"name":"stratum","version":"0.6.1"}`. |

Verification: named-test selection **22 passed**; additional targeted terminal/cleanup/transport checks **13 passed**; opt-in verification of existing `requests2` evidence **9 passed** (eight overlap the named selection). No model calls, build, full suite, or repository-file edits. The buffered-exit reproduction fails as described. No additional double-claim, pause/resume deadlock, or cleanup regression identified within this scope.

Live evidence: `/private/tmp/claude-501/-Users-ruze-reg-my-forge/a0b33df7-0e94-469a-b181-8b5ea924ff6d/scratchpad/peer3-evidence/requests2/`.
